# WilliamO-C.github.io

Personal site — source at `index.html` / `style.css` / `script.js`, no build step. Served directly by GitHub Pages from `main`.

Run locally:

```bash
python3 -m http.server 8000
# open http://localhost:8000
```
